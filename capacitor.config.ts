import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ramdiaz.app',
  appName: 'MAVRD',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
